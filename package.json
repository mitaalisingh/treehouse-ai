{
  "name": "treehouse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:migrate": "pnpm --filter @treehouse/api db:migrate",
    "db:studio": "pnpm --filter @treehouse/api db:studio",
    "db:seed": "pnpm --filter @treehouse/api db:seed"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
